<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.ReplyMapper">

	<!-- 사용자 정의 resultMap 요소 정의 -->
	<resultMap type="Reply" id="replyMap">
		<result property="replyNo" column="reply_no" />
		<result property="userNo" column="user_no" />
		<result property="boardNo" column="board_no" />
		<result property="content" column="content" />
		<result property="regDate" column="reg_date" />
		<collection property="member" resultMap="memberMap">
		</collection>
	</resultMap>
	<resultMap type="Member" id="memberMap">
		<result property="userNo" column="user_no" />
		<result property="userId" column="user_id" />
	</resultMap>

	<!-- 댓글 등록 -->
	<insert id="register">
		insert into REPLY VALUES
		(REPLY_SEQ.NEXTVAL,#{userNo},#{boardNo},#{content},SYSDATE)
	</insert>

	<select id="list" resultMap="replyMap">
		SELECT R.REPLY_NO,
		USER_ID,R.CONTENT,R.REG_DATE
		FROM MEMBER M INNER JOIN REPLY R ON
		M.USER_NO = R.USER_NO INNER JOIN
		BOARD B ON B.BOARD_NO = R.BOARD_NO
		WHERE B.BOARD_NO = #{boardNo} ORDER
		BY
		R.REPLY_NO ASC
	</select>

	<!-- 댓글 삭제 -->
	<delete id="replyRemove">
		DELETE FROM REPLY WHERE REPLY_NO =
		#{replyNo}
	</delete>

	<update id="update">
		UPDATE REPLY SET CONTENT = #{content} WHERE reply_no
		= #{replyNo}
	</update>




</mapper> 





